//given an array of intergers, reurn true if all the values frequenices are unique (each value appears some number of times, and no two values appear the same number of times; return true iff no two distinct values share the same frequency)
#include <stdio.h>
#include <stdbool.h>

bool uniqueOccurences(int arr[], int n){
    for(int i=0; i<2001; i++){
    }
}